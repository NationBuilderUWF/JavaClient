package QuestionAnswer;

//import WebUtilities.AdminQuestionReq;
import Map.Nation;

/**
 * Created by crims_000 on 4/1/2016.
 */
public class QuestionAnswerController {
    /*
}

public AdminQuestionReq pullQuestion(AdminQuestionReq bank[]){
    int id = (int)Math.random() * bank.length;
    AdminQuestionReq query = database.queryQuestionBank(id);
    return query;
}

public void answerQuestion(AdminQuestionReq question, StudentQuestionReq answer, Nation rewarded){
    if(question.adminCorrectAnswer == answer.studentCorrectAnswer){
        boolean success = database.addNationResources(rewarded);
    }
    */
}